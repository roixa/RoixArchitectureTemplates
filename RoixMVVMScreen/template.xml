<template format="5"
		revision="1"
		name="Screen"
		description="Creates a new MVVM Screen">
 
	<category value="Roix MVVM Architecture"/>
 

	<parameter
		id="screenName"
		name="Screen Name"
		type="string"
		constraints="class|unique|nonempty"
		default="Main"
		help="The name of the first screen (activity)." />

    <parameter
        id="addToolbar"
        name="Add toolbar to screen"
        type="boolean"
        default="false"
        help="Add toolbar to screen fragment."/>

    <parameter
        id="useExistingViewModel"
        name="Use existing ViewModel?"
        type="boolean"
        default="false"
        help="Use existing ViewModel and their scope."/>


	<parameter
		id="viewModelName"
		name="ViewModel Name"
		type="string"
		constraints="class|unique|nonempty"
		default="Feature"
		visibility="useExistingViewModel"
		help="The name of the existing providing repository." />

	<parameter
        id="notGenerateRepository"
        name="Without repository"
        type="boolean"
        visibility= "!useExistingViewModel"
        default="false"
        help="Do you want not generate Repository?"/>


	<parameter
        id="notGenerateInteractor"
        name="Without interactor"
        type="boolean"
        visibility= "notGenerateRepository"
        default="false"
        help="Do you want not generate Interactor?"/>

	<globals file="globals.xml.ftl" />

    <execute file="recipe.xml.ftl" />

</template>